

(() => {
    // Comillas simples
    const batman: string = 'batman';
    // Comillas dobles
    const linternaVerde: string = "Héroe: LInterna Verde";
    // Backticks
    const catwoman: string = `Catwoman`

    console.log( batman.toUpperCase()  );
    console.log( batman[10]?.toUpperCase() );
})()